# ============================================================
# PostgREST Configuration — Oratioo CX
# Conecta al VPS (srv.oratioo.com)
# ============================================================

db-uri = "postgresql://authenticator:oratioo_authenticator_2024@srv.oratioo.com:5432/oratioo"

db-schemas = "public"
db-anon-role = "web_anon"

jwt-secret = "oratioo-local-jwt-secret-change-in-production-2024"

server-host = "0.0.0.0"
server-port = 3001

server-cors-allowed-origins = "http://localhost:5173,http://localhost:3000"

db-pool = 10
db-pool-timeout = 10
db-max-rows = 1000
